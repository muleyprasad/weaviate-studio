# Weaviate Studio Sandbox – Environment Variables
#
# Copy this file to .env and replace the placeholder with your real key:
#   cp .env.example .env
#
# The OpenAI API key is ONLY needed for generative / RAG queries.
# Data import and vector search use the local transformers sidecar (free).
OPENAI_API_KEY=your-openai-key-here
